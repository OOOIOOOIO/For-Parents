spring:
  config:
    activate:
      on-profile: real
  datasource:
    url: jdbc:mysql://localhost:3306/test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: test
    password: test
    #    url: jdbc:mysql://34.123.145.68:3306/wero?useSSL=false&allowPublicKeyRetrieval=true&&characterEncoding=UTF-8&serverTimezone=UTC
    #    username: wero
    #    password: wero
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 500
        dialect:
          storage_engine: innodb

